# Set in Lambda configuration (Environment variables)
# Must match backend's MATCHMAKING_API_SECRET

START_GAME_SECRET=

# Required for leave action - HMAC secret to sign leave tokens (prevents arbitrary player removal)
LEAVE_TOKEN_SECRET=
