# Server port
PORT=8080

# AWS (required for DynamoDB lobby validation)
# Set region to match your DynamoDB table (e.g. us-west-2)
AWS_REGION=us-west-2
# Credentials: use AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, or ~/.aws/credentials
